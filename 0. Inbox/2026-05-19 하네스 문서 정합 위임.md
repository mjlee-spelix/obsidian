# 위임 프롬프트 — 하네스 문서 정합 (5/18 청산 + 5/19 사고 학습 반영)

> 작성일: 2026-05-19
> 작업 위치: `C:\Users\Administrator\Projects\spx-agent\.claude\`
> 위임 범위: hdd/ + references/ 정합 보강 + 위임 프롬프트 표준 강화
> 선행 작업: 5/18 마트 백엔드 service 재작성 + Shadow DB 부채 청산 / 5/19 keycloak·dify-audit 컨테이너 사고 수습

---

## 배경 — 문서가 뒤처진 학습 5축

5/15~5/19 누적된 사고/학습이 코드/DB엔 반영됐는데 **hdd 문서엔 반영 안 됨**. 다음 위임 작업(목업 데이터 보강) 던지기 전에 가드 박아야 동일 사고 재발 방지 효력 발생.

### 누적 학습 (시간 순)
1. **5/15 audit rename 사고** — design.md 본 갱신 ↔ Prisma 마이그레이션 후속 작업 분리 (drift 누적 패턴)
2. **5/18 마트 drift 1차** — enriched view `tenant_id::uuid` 캐스트 누락 (design.md ↔ 실제 DB DDL disconnect)
3. **5/18 마트 drift 2차** — `actor_id` 비-UUID 안전 처리 (`text = uuid` operator 에러)
4. **5/18 Shadow DB 부채 청산** — 갈래 1(entrypoint baseline) + 갈래 2(idempotent) 합성 솔루션. Prisma 약점 영역(RENAME / 스키마 이동)에 무리하게 박은 학습
5. **5/19 컨테이너 옛 정의 보존 사고** — keycloak realm 파일 mount 옛 경로 잔존 (restart로는 새 compose 반영 안 됨) + dify-audit dist 옛 빌드 잔존 (collector raw query `accounts` → `spx_accounts` 미반영)

---

## 진입점 (순서대로 읽기)

1. `CLAUDE.md` — 프로젝트 개요 + 아키텍처 불변식
2. `SESSION_HISTORY.md` 2026-05-18 / 2026-05-19 entry — 사고 + 청산 상세
3. `hdd/design.md § 2.5.4` — 마트 DDL 정식본 (5/18 보정 3건 본 갱신 반영 확인)
4. `hdd/quality-criteria.md` — 자기 평가 기준 (현 § 2 백엔드 영역 확인)
5. `hdd/defect-catalog.md` — 기존 결함 패턴 (H-DASH-XX / H-ENV-XX) ID 규칙 파악
6. `references/audit-schema.md` — audit_events 컬럼 정의
7. `references/dify-db-schema.md` — Dify 원천 테이블 (`accounts` → `spx_accounts` rename 확인)
8. `references/rbac-schema.md` — RBAC 표준 (prefix 없음)

---

## 작업

### 작업 A — 사전 분석

- A.1 `hdd/design.md § 2.5.4` 본문 — 5/18 보정 3건(tenant_id::uuid 캐스트 / actor_id CASE WHEN / is_canonical_call 표현식)이 본 갱신에 박혀있는지 verbatim 확인
- A.2 `hdd/defect-catalog.md` 기존 ID 규칙 파악 (H-DASH-XX / H-ENV-XX) + 다음 시작 번호
- A.3 `hdd/quality-criteria.md § 2` 백엔드 영역 현재 게이트 항목 목록
- A.4 위임 프롬프트 템플릿이 어디 박혀있는지 (별도 파일 / CLAUDE.md 안 / hdd/ 안) 위치 파악

### 작업 B — design.md § 2.5.4 정합 재확인 + 보정 (필요 시)

- 5/18 보정 3건이 이미 본 갱신에 박혔는지 verbatim 확인
- 누락 시 본 갱신 + "5/18 보정" 주석으로 박기
- enriched view DDL 코드 블록이 실제 마이그레이션 본문(`20260518xxxxxx_fix_enriched_tenant_id_cast` + `20260518100000_actor_id_non_uuid_safe`)과 1:1 정합 확인

### 작업 C — quality-criteria.md § 2 백엔드 끝에 "drift 게이트" 한 줄 추가

```markdown
- [ ] **drift 게이트** — design.md § 2.5.4 DDL 코드 블록 vs 실제 DB DDL dump 1:1 정합 (`scripts/check-mart-drift.ps1` 활용). 불일치 발견 시 작업 중단 + 본 갱신/마이그레이션 후속 작업 동시 처리
```

### 작업 D — defect-catalog.md 신규 결함 5종 등록

기존 ID 규칙 따라 (H-MART-XX / H-INFRA-XX / H-DOC-XX 신설 카테고리 가능):

| 카테고리 | 패턴 요약 | 검증/방어 |
|---|---|---|
| H-MART-XX | 비-UUID actor 캐스트 함정 (`text = uuid` operator 에러). enriched view에서 `actor_id::uuid` 직접 캐스트 시 `actor_type IN ('api','system')` 행 (UUID 아닌 문자열)에서 깨짐 | `CASE WHEN actor_type IN ('account','end_user') THEN actor_id::uuid ELSE NULL END` 패턴 + downstream JOIN에 `actor_type='account'` 가드 |
| H-INFRA-XX | Shadow DB 부채 — Prisma 마이그레이션이 RENAME / 스키마 간 이동 / 운영-신규 환경 출발 상태 차이를 한 마이그레이션에 담으면 `migrate dev` Shadow DB 재생 불가 → `mkdir + resolve --applied` 우회 강요 → 부채 누적 | 첫 마이그레이션부터 idempotent (`IF NOT EXISTS` / `CREATE OR REPLACE` / `DO $$` 조건부 데이터 이전) + entrypoint baseline 분기 (`_prisma_migrations` 부재 시 자동 적용) 합성 |
| H-INFRA-XX | Docker 컨테이너 옛 mount 정의 보존 — compose volume 경로 변경 후 `restart`로는 새 정의 반영 안 됨. 옛 경로 호스트에 없으면 Docker Desktop이 빈 디렉토리 자동 생성 → "디렉토리를 파일에 마운트" 에러 | compose 변경 후 `docker compose up -d --force-recreate <service>` 필수. 빈 디렉토리 잔존 시 수동 `Remove-Item -Recurse` |
| H-INFRA-XX | dist 빌드 캐시 함정 — TS 컴파일 결과물(`dist/`)이 컨테이너 이미지에 박힘. src 변경(특히 raw query 박힌 collectors) 후 `restart`만으론 옛 dist 그대로. `build` + `--force-recreate` 필수 | 동료 안내 시 `git pull → build → up -d --force-recreate` 3종 세트로 박기. restart 단독 금지 |
| H-DOC-XX | design.md 본 갱신 ↔ Prisma 마이그레이션 후속 작업 분리 (drift 누적 패턴) — 본 갱신만 박히고 마이그레이션 후속 누락. 5/15 audit rename / 5/18 drift 1·2차 모두 같은 가족 | 본 갱신 PR 시 마이그레이션 파일 동시 commit 의무화. drift 게이트(작업 C)로 사후 감지 |

### 작업 E — references 정합 보강

- E.1 `references/audit-schema.md` — `actor_id` 컬럼 정의에 actor_type별 형식 명시:
  - `account` / `end_user` → UUID 형식
  - `api` → 문자열 (e.g. `'chatbot-public-001'`)
  - `system` → 문자열 (`'system'`)
  - 마트 캐스트 시 H-MART-XX 가드 참조
- E.2 `references/dify-db-schema.md` — `accounts` → `spx_accounts` rename 반영 (5/18 권대리님 `f88f4a6`). 컬럼은 동일, 테이블명만 변경. dify-audit collector raw query도 모두 `spx_accounts` 사용
- E.3 `references/rbac-schema.md` — `spx_accounts` rename에 따른 RBAC FK/JOIN 영향 검토. RBAC 5종(`departments` / `department_members` / `resource_ownership` / `resource_permissions` / `rbac_audit_logs`)은 회사 표준 prefix 없음 유지

### 작업 F — 위임 프롬프트 표준 강화

위치: 별도 `hdd/delegation-standard.md` 신설 또는 기존 위임 템플릿에 박기

필수 박을 가드:
1. **drift 발견 시 작업 중단 의무** — design.md ↔ 실제 DB DDL 불일치 발견 시 자동 보정 금지, 사용자 보고 후 결정 받기
2. **자가 검증 거짓 가능성 가드** — "완료" 보고 전 실제 출력으로 직접 확인 (`\dt` / `ls 파일경로` / endpoint hit 결과). 5/15·5/18 학습 재발 방지
3. **마이그레이션 본문 작성 표준** — idempotent (`IF NOT EXISTS` / `CREATE OR REPLACE` / `DO $$` 조건부) 의무. RENAME / 스키마 이동 시 `DO $$` 조건부 데이터 이전 패턴
4. **컨테이너 변경 시 명령 세트** — entrypoint / compose / dist 변경 후 `restart` 단독 금지, `build + up -d --force-recreate` 의무
5. **PowerShell 한글 인코딩 함정 회피** — 한글 SQL 본문 작성 시 UTF-8 BOM 없이 + docker compose cp + psql -f 패턴. `Get-Content -Raw | docker exec -T` 금지

---

## 검증 (필수)

### V.1 design.md § 2.5.4 verbatim 확인
- 5/18 보정 3건이 본 갱신에 박혀있는지 grep으로 확인
- DDL 코드 블록이 실제 마이그레이션과 1:1 정합

### V.2 defect-catalog.md ID 일관성
- 신규 5종이 기존 ID 규칙 충돌 없음
- 각 항목에 "검증/방어" 필드 박힘 (단순 패턴 설명만 박지 말 것)

### V.3 위임 프롬프트 표준 가드 effectiveness
- 가상 시나리오 3종 (drift 발견 / 자가 검증 거짓 / restart 후 안 됨)에서 가드가 어떻게 작동할지 시뮬레이션 후 본문 박기

### V.4 references 정합
- `spx_accounts` rename 후 모든 references 문서 grep — `\baccounts\b` 잔존 위치 0건 (Dify 기본 테이블 설명 맥락 제외)

---

## 출력 (작업 결과 보고)

`SESSION_HISTORY.md` 2026-05-19 entry에 추가:

1. **작업 A 분석 결과**: 기존 문서 상태 + 보정 필요 항목 매트릭스
2. **작업 B design.md 보정 결과**: 변경 라인 수 + verbatim 확인 결과
3. **작업 C quality-criteria 추가 결과**: drift 게이트 한 줄 박힘
4. **작업 D defect-catalog 신규 5종**: 각 ID + 검증/방어 본문
5. **작업 E references 보정 결과**: 파일별 변경 요약
6. **작업 F 위임 프롬프트 표준**: 위치 + 가드 5종 본문
7. **검증 V.1~V.4 실측 결과**: 각 grep / 시뮬레이션 출력
8. **다음 위임(목업 데이터 보강)에 적용될 가드 요약**: F 가드가 PROMPT.md 작업 시 어떻게 작동할지

---

## 진행 금지

- **마트 백엔드 service 코드 변경 금지** — 5/18 청산 완료 영역, 문서만 정합
- **마이그레이션 파일 신설 금지** — design.md ↔ 마이그레이션 정합은 작업 B의 verbatim 확인만, 신규 마이그레이션은 별도 작업
- **defect-catalog 기존 항목 변경 금지** — 신규 5종 추가만
- **PowerShell 한글 인코딩 함정 재발 금지** — 5/18 학습 적용
- **자가 검증 거짓 금지** — "완료" 보고 전 실제 출력 첨부

---

## 5/18·5/19 학습 적용 (반드시 의식)

- **자가 검증 거짓 가능** — 문서 박았다고 보고 전 `grep` 또는 직접 파일 열어 확인
- **drift 게이트 본인 적용** — design.md ↔ 실제 DB DDL 정합 작업 자체가 drift 게이트 본 작업
- **위임 프롬프트 가드 effectiveness 검증** — 박은 가드가 실제 다음 위임에서 작동할지 시뮬레이션 의무

---

작업 위치: `C:\Users\Administrator\Projects\spx-agent\.claude\`
