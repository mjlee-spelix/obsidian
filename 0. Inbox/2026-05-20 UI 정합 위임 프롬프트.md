# 위임 프롬프트 — UI 정합 14개 항목 (PPTX 화면 수정 피드백 청산)

> 작성일: 2026-05-20
> 작업 위치: `C:\Users\Administrator\Projects\spx-agent\`
> 위임 범위: **공통/메인/KPI 1·2·4 화면의 UI 정합 14개 항목** (폰트/간격/정렬/ellipsis/i18n/추세 기호/active ring). 백엔드 변경 0건, spec 갱신 포함
> 선행: 5/20 KPI 2/4 정합 완료 + 5/20 KPI 1+3 정합 별도 진행 중 + 5/20 시드 재구성 완료
> 가드: `hdd/delegation-standard.md` § 1·2·5 적용

---

## 배경 — 사용자 화면 검토 피드백 청산

사용자가 dev 환경에서 화면 점검 후 [[0. Inbox/2026-05-20 화면 수정 피드백.pptx]]에 14개 UI 수정 항목 박음. 시각/일관성/UX 영역만 (기능 변경 0건). KPI 1+3 정합 위임과 별도 트랙.

---

## 작업 범위 매트릭스 — 14개 항목

| # | 화면 | 수정 내용 | 적용 범위 | 권장 구현 |
|---|---|---|---|---|
| **1.0** | 공통 | **폰트 스타일 통일** (제목/부제목/컬럼명/필드값) — 탐색/스튜디오/모니터링 기준 | 전체 | `web/app/(commonLayout)/explore/`, `web/app/(commonLayout)/apps/`, `web/app/components/app/overview/` 폰트 토큰 추출 → KPI/drill 영역 적용 |
| **1.1** | 공통 | 대시보드 설명 문장 1~2줄 추가 (탐색 화면 패턴) | 전체 | 페이지 헤더 아래 `<p className="text-text-tertiary text-sm">...</p>` 추가. i18n key 신설 |
| **1.2** | 공통 | 긴 텍스트 ellipsis + hover 전체 보기 | 모든 차트 | Tailwind `truncate` + `title={fullText}` 또는 Radix Tooltip. 막대 라벨 / 표 셀 / Top 앱명 등 |
| **1.3** | 메인 | 합계 위치 변경 | 메인 | ⚠️ **PPTX 상세 없음** — 위임 측이 화면 캡처 + 사용자 확인 후 진행 (현 위치 / 권장 안 2~3개 제시) |
| **1.4** | 공통 | 행 수 고정 — 부서별 오브젝트 **6** / 모델별 토큰 **6** / 부서별 활동 **10** + 카드 크기 고정 | 모든 차트/표 | 5/13 박힌 "고정 높이 + 내부 스크롤" 정책 적용. `max-h-[N]` + `overflow-y-auto` |
| **1.5** | 공통 | 차트/표 카드 간격 확대 (모니터링 수준), **KPI 간격은 현행 유지** | 모든 차트/표 | 모니터링 페이지(`web/app/components/app/overview/`)의 `gap-X` 토큰 추출 후 차트/표 그리드 적용. KPI 그리드는 변경 X |
| **1.6** | 공통 | 차트 값 **내림차순** + 동률 시 **이름순** (a→z, ㄱ→ㅎ) | 모든 차트 | 백엔드 ORDER BY 또는 프론트 sort. 2차 정렬 `localeCompare('ko-KR')` |
| **1.7** | 공통 | 표 컬럼 간격 균일 | 모든 표 | `grid-cols-N` 또는 `<th width="N%">` 균등 분배 |
| **1.8** | 메인 | 특정 요소 제거 | 메인 | ⚠️ **PPTX 상세 없음** — 위임 측이 화면 캡처 + 사용자 확인 후 진행 |
| **1.9** | 공통 | 언어별 라벨 — 한글: **앱/지식/도구**, 영어: **App/KB/Tool** | 모든 화면 | `web/i18n/ko-KR/dashboard.ts` + `en/dashboard.ts` 정합. `app/kb/tool` 라벨 i18n key 통일 |
| **2.1** | 총 오브젝트 | KPI 카드 클릭 시 테두리 잘림 해결 | 모든 KPI 카드 | **`ring-inset` 적용** (권장). `cn(base, isActive && 'ring-inset ring-2 ring-components-button-primary-bg-hover bg-components-card-bg-hover')` — 카드 안쪽 ring으로 잘림 0, 레이아웃 무영향 |
| **2.2** | 총 오브젝트 | 차트 간격/막대 너비 메인 통일 | 모든 차트 | 메인 화면 막대 차트 `gap-X` / `barWidth` 토큰 추출 후 drill 차트 적용 |
| **4.1** | API 호출 | **변동률 기호 변경**: `▼28.6%` → `▾ 28.6%` (양수 `▴ 28.6%`) | KPI 카드 증감 + drill 표 추세 컬럼 전수 | `▲▼` → `▴▾` 일괄 치환. `kpi-cards.md § 증감 뱃지` 표 갱신 + `TrendBadge` 컴포넌트 |
| **5.1** | 앱별 통계 | 차트 이름 변경 — **"에러율 상위 10개 앱"** (좌하 차트 "앱별 호출 수 Top 10"도 **"호출 수 상위 10개 앱"** 으로 대칭 정합) | 앱별 통계 | `web/app/components/admin/drill-charts/apps/top-error-apps.tsx` + `app-call-top10.tsx` + spec design.md § 영역 매핑 line 70 + § apps 표 좌하/우하 명세 갱신 |

**비범위 (이번 위임 안 함)**:
- 백엔드 service 코드 변경 (UI/UX 전용)
- KPI 1+3 drill-through 정합 (별도 위임 진행 중)
- KPI 2/4 재변경 (5/20 완료)
- 시드 데이터 변경 (5/20 완료)
- spec frontmatter `harness: [...]` 변경
- 새 컴포넌트 신설 (CSS / i18n / 기존 컴포넌트 정합만)
- chart-drawer 보류 정책 변경 (H-DASH-20 유지)

---

## 사용자 추천안 채택 (2건)

본 위임 발사 전 사용자가 결정한 권장안:

### 2.1 — `ring-inset` 채택
- 카드 안쪽에 ring 표시 → 부모 컨테이너 padding 무관 → **잘림 0 + 레이아웃 변동 0**
- 한 줄 변경: `ring-2` → `ring-inset ring-2`
- 다른 대안(카드 간격 늘리기 / outline / shadow)보다 단순성 + 무영향

### 5.1 — "에러율 상위 10개 앱"
- 직관적 + 한국어 자연 + 정렬 기준(에러율) 명시
- 좌하 차트도 대칭 정합 — "앱별 호출 수 Top 10" → **"호출 수 상위 10개 앱"**
- spec design.md § 영역 매핑 line 70 + apps 표 명세 동기 갱신

---

## 진입점 (순서대로 읽기)

1. `CLAUDE.md` — 프로젝트 개요
2. [[0. Inbox/2026-05-20 화면 수정 피드백.pptx]] — 원본 PPTX (참고). 본 프롬프트 표가 단일 진실
3. `SESSION_HISTORY.md` 2026-05-20 entries — 5/20 작업 흐름 (KPI 2/4 / 부하 테스트 / 시드 / spec 갱신)
4. **`hdd/specs/requirements/kpi-cards.md` § 증감 뱃지** — 4.1 추세 기호 적용 base
5. **`hdd/specs/design/kpi-drill-through.md` § 영역 매핑** + **§ apps 표 명세** — 5.1 차트 이름 변경 적용
6. **`hdd/specs/requirements/kpi-drill-through.md` § 고정 높이 정책** — 1.4 행 수 고정 base
7. `web/app/components/header/dashboard-nav/` — 메인 헤더 패턴 (1.0 폰트 토큰 추출 base)
8. `web/app/components/app/overview/` — 모니터링 페이지 (1.5 카드 간격 base)
9. `web/i18n/` — 1.9 라벨 정합 base
10. `hdd/delegation-standard.md` § 1·2·5

---

## 작업 단계

### 1. 폰트/간격 토큰 추출 (1.0 / 1.5)
- 탐색/스튜디오/모니터링 화면 본문 정독 → 폰트 클래스 / `gap-X` 토큰 매트릭스 만들기
- KPI/drill 영역 현 토큰과 비교 → drift 매핑
- 사용자 보고 후 표준 토큰 확정 + 일괄 적용

### 2. 공통 UI 정합 (1.2 / 1.4 / 1.6 / 1.7)
- ellipsis + tooltip — 막대 라벨 / 표 셀 / Top 앱명 등 적용 위치 전수 grep
- 행 수 고정 — 부서별 오브젝트 6 / 모델별 토큰 6 / 부서별 활동 10 + `max-h` 박음
- 정렬 — 모든 차트 데이터 fetching hook (`use-admin-drill.ts` 등) 또는 컴포넌트에서 `localeCompare` 2차 정렬 박음
- 표 컬럼 균등 — `grid-cols-N` / `<th width>` 정합

### 3. i18n 정합 (1.9)
- `web/i18n/ko-KR/dashboard.ts` + `en/dashboard.ts` (또는 현 구조)에 `'app': '앱'` / `'kb': '지식'` / `'tool': '도구'` 매핑 박음
- 영어는 `App / KB / Tool` 그대로 유지
- 적용 위치 grep 후 일괄 치환

### 4. 메인 화면 (1.1 / 1.3 / 1.8 / 1.5 KPI 제외)
- 1.1 설명 문장 — 탐색 화면 패턴 복제 + i18n key 신설
- 1.3 / 1.8 — **PPTX 상세 없음, 위임 측이 화면 캡처 + 사용자 보고 후 진행**

### 5. KPI 카드 (2.1 / 4.1)
- 2.1 — `kpi-card.tsx`의 active className에 `ring-inset` 추가
- 4.1 — `TrendBadge` 컴포넌트 또는 증감 표시 코드에서 `▲` → `▴`, `▼` → `▾` 일괄 치환 (drill 표 추세 컬럼 포함)

### 6. KPI 1 (2.2)
- 차트 간격/막대 너비 — `dept-cumulative.tsx` + 메인 `dept-objects` 차트 토큰 비교 → 통일

### 7. KPI 4 (5.1)
- `top-error-apps.tsx` 컴포넌트 제목 — `"에러율 상위 10개 앱"`
- `app-call-top10.tsx` 컴포넌트 제목 — `"호출 수 상위 10개 앱"`
- spec design.md § 영역 매핑 line 70 + § apps 표 명세 동기 갱신

### 8. spec 갱신 (필요 시)
- `hdd/specs/design/kpi-drill-through.md`:
  - § 영역 매핑 line 70 — apps 차트 이름 갱신
  - § apps 표 명세 — 좌하/우하 차트 이름 갱신
- `hdd/specs/requirements/kpi-drill-through.md`:
  - § 고정 높이 정책에 1.4 행 수 명시 추가
  - § 시각 강조에 `ring-inset` 명시
- `hdd/specs/requirements/kpi-cards.md`:
  - § 증감 뱃지 — `▲▼` → `▴▾` 갱신
- 신규 영역 (있으면 추가):
  - § 정렬 규칙 — 내림차순 + 이름 2차 정렬 패턴
  - § i18n 라벨 — 앱/지식/도구 ↔ App/KB/Tool 매핑

### 9. 테스트 갱신
- 변경 컴포넌트 vitest 케이스 갱신 (KPI 카드 active ring / TrendBadge 기호 / TopErrorApps 제목)
- 시각 회귀 테스트 있으면 스크린샷 갱신

---

## 검증 (필수, raw 출력 첨부 의무 — delegation-standard § 2)

### V.1 14개 항목 적용 매트릭스
- 항목별 적용 위치 / 변경 내용 / 정합 결과 표 (Before / After 코드 diff 또는 클래스명)
- 1.3 / 1.8 PPTX 상세 부재 항목은 사용자 보고 결과 박음

### V.2 폰트/간격 토큰 정합
- 탐색/스튜디오/모니터링 토큰 vs KPI/drill 토큰 비교표
- 적용 후 1:1 일치 확인

### V.3 ring-inset 시각 (2.1)
- KPI 카드 4종 active 시 ring 잘림 0 확인 (스크린샷 또는 vitest className 검증)

### V.4 추세 기호 정합 (4.1)
- 코드 grep `'▲|▼'` 0건 확인 (`▴▾`로 전수 치환)
- KPI 카드 증감 + drill 표 추세 컬럼 둘 다 적용

### V.5 정렬 동작 (1.6)
- 모든 차트 데이터 정렬: 내림차순 + 동률 시 이름 정렬
- 한국어 정렬 (`localeCompare('ko-KR')`) 적용 검증

### V.6 i18n 라벨 토글 (1.9)
- 한국어: 앱/지식/도구 표시
- 영어: App/KB/Tool 표시
- 토글 시 모든 화면 일관 변경

### V.7 행 수 고정 (1.4)
- 부서별 오브젝트 6 / 모델별 토큰 6 / 부서별 활동 10 행 보임 + 나머지 스크롤
- 카드 크기 데이터 양 무관 고정

### V.8 카드 간격 (1.5)
- 차트/표 카드 간격 모니터링 수준 적용
- **KPI 카드 그리드 간격 현행 유지** (변경 0 검증)

### V.9 차트 이름 (5.1)
- KPI 4 좌하 = "호출 수 상위 10개 앱"
- KPI 4 우하 = "에러율 상위 10개 앱"
- spec design.md 동기 갱신 확인

### V.10 vitest 통과
- 변경 컴포넌트 단위 테스트 raw 출력
- `pnpm vitest run app/components/admin/`

### V.11 메인 1.3 / 1.8 사용자 보고
- 화면 캡처 + 권장 안 2~3개 제시 + 사용자 결정 기록

---

## 진행 금지

- **백엔드 service 코드 변경 금지** (UI/UX 전용)
- **KPI 1+3 drill-through 정합 변경 금지** (별도 위임 진행 중)
- **KPI 2/4 코드 재변경 금지** (5/20 완료, 보존)
- **시드 데이터 변경 금지** (5/20 완료)
- **KPI 카드 그리드 간격 변경 금지** (1.5 명시 — KPI 간격은 현행 유지)
- **새 컴포넌트 신설 금지** — CSS / i18n / 기존 컴포넌트 정합만
- **chart-drawer 보류 정책 변경 금지** (H-DASH-20 유지)
- **PM 영역 임의 결정 금지** — 1.3 합계 위치 / 1.8 메인 제거 요소는 사용자 보고만, 임의 결정 X
- **spec 본문 우회한 임의 명세 작성 금지** — drift 발견 시 사용자 보고 (delegation-standard § 1)
- **자가 검증 거짓 금지** — "정합 완료" 보고 전 V.1~V.11 raw 출력 첨부 (delegation-standard § 2)
- **PowerShell 한글 인코딩** — `Set-Content -Encoding UTF8` + `docker compose cp` 패턴 (delegation-standard § 5)

---

## 출력 (작업 결과 보고)

`SESSION_HISTORY.md` 2026-05-20 entry 추가:

1. **14개 항목 적용 매트릭스** — 항목별 Before / After 표
2. **폰트/간격 토큰 매트릭스** — 탐색/스튜디오/모니터링 vs KPI/drill 정합 결과
3. **spec 갱신 diff 요약** — design / requirements / kpi-cards 변경 라인
4. **컴포넌트 변경 파일 목록** — 프론트 N파일 + i18n M파일
5. **검증 V.1~V.11 raw 출력**
6. **1.3 / 1.8 사용자 보고 결과** — 화면 캡처 + 권장 + 사용자 결정 기록
7. **다음 작업 안내**:
   - KPI 1+3 정합 위임 진행 상태 확인 ([[0. Inbox/2026-05-20 KPI 1 + KPI 3 drill-through 정합 위임 프롬프트.md]])
   - 시각 회귀 테스트 추가 권장 여부
   - 메인 화면 디자이너/PM 컨펌 필요 항목 (있으면)

---

## 5/11~5/20 학습 적용 (반드시 의식)

- **자가 검증 거짓 가능성** — V.1~V.11 raw 출력 첨부 의무
- **drift 게이트** — PPTX 14개 항목 / spec / 코드 disconnect 발견 시 작업 중단 + 사용자 보고
- **PowerShell 한글 인코딩** — i18n 파일 / 한국어 라벨 적용 시 mojibake 회피
- **임의 결정 금지** — 1.3 / 1.8 PPTX 상세 부재 영역은 사용자 보고만 (delegation-standard § 1)
- **컨테이너 build 잔존** — 프론트 변경 시 web 컨테이너 재빌드 확인 (H-INFRA-02·03)

---

작업 위치: `C:\Users\Administrator\Projects\spx-agent\`

---

## 사용 메모

- KPI 1+3 정합 위임 진행 중이라 PROMPT.md 점유 중일 수 있음 → 본 위임은 별도 슬롯 또는 KPI 1+3 완료 후 진입
- 백엔드 변경 0건이라 KPI 1+3 위임과 동시 진행도 충돌 작음 (단 spec 파일 동시 편집은 git conflict 위험 — 순차 진행 권장)
