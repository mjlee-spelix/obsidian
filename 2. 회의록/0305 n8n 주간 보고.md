>시나리오 세부 내용 숙지 되어야
  ui 통합하면서 세부 설정 내용 익혀두기
 기능 구현이 우선

research-report

legacy(erp-sap)
- http 로 sandbox 개인 정보를 가져와서 salesforce랑 postgres에 저장 후 저장 결과를 email로 보냄
- 레거시 연계가 된다는 컨셉의 시나리오
- smtp 가 send an email 노드임
- 가져오는 사원 번호는 랜덤임
- 시나리오 하나로 sap랑 salesforce 다 붙인 거?

---

메일 from 코난

hitl이 n8n 노드로 하게 되면? dify에서 n8n이랑 연계해야 되는데 사용자가 요청했을 때 이게 hitl->dify->n8n->dify가 되나?
- **hitl을 워크플로우 상에선 어떻게 구현 가능한지 찾아보기**
- **dify에서 받아서 n8n 워크플로우로 전달하고 등.....의 flow** 
- n8n을 함수처럼 구현하는 방안? 은 dify에서 다 구현하면 되는데 굳이..

사내 규정 검색
- '근거 부족' 이란 기준을 어떻게 정해야 되나
- rag 검색 3번했는데도 사용자 질의에 만족하지 못하면 어떤 답변을 해라~ 라고 고정된 답변 출력 되게끔 설정해두기?
- llm judge 붙여서 질문-근거-응답 을 평가해서 누락됐다면 다시 agent 붙이거나 재질의하거나
- '근거 부족'이 rag 결과인지? 사용자 질문인지?
	- rag 결과인 듯
	- 어떻게 부족하다고 판단? 할루시네이션?
	- rag 자체에 답변이 없는 경우
- 개인화 AI agent
	- 개인 자료/공용 자료
	- **개인만 접근 가능하게, 공동 접근 가능하게 권한 관리 가능** 
	- dify 계정을 사용자에게 열어줘야 되는건지, client 만들고 webhook으로 지식에 접근 가능하게 해야 할지? 음?
	- client에서 workflow 통해서 지식 생성하는 걸 원하는 거 같음
	- dify 지식을 n8n에서 쓰는 건 고려 안해도 됨
	- dify에서 지식 만들고 client에서 호출 되는지

실시간 매출 분석
- 시각화 도구 연결
- n8n에서 하게 되면 dify 거쳐서 client 에서 보여주는 게 되는지?
- **n8n에 시각화노드가 있는지 확인하고 dify->client까지 되는지 확인하기** 
- n8n에서 안되면 dify에서 시각화하는 게 일반적일 거 같음
- 암튼 차트 그려주는 노드 찾아보기

레거시 연동
- 전자결재 상신/승인? 예를 들어서, 휴가 신청을 대리에게 전자 결재 상으로 요청하면 레거시 연동해서 보낸 사람 결재 요청 문서함에서 확인하고 결재하고 그룹웨어 호출해서 누가 상신했고 등을 판단해서 
- 그룹웨어 api로 n8n에서 전자결재 연동
- 전자결재 ui는 따로 있음...........?
- 전자결재 어떻게 보여주고 api를 어떻게 왔다갔따할지
- agent가 왜 들어가야 되나?? 그룹웨어가 있는데???
- 코파일럿 형태로 구현해야..................??
- 결재를 tool로 만들어서 agent에붙이기, 승인 여부는 agent가 판단하지 않게 
- 승인 요청을 agent가 작성해서 (연차 이메일을 작성해서 상신 보내기)
- 이런 게 문서 안에 내용이 들어가야 됨
- 이질적인 시스템으로 가면 일이 커지니까 기존 서비스에 도입하는 코파일럿 형태로..
- 이걸 어느정도 수준의 구현이 필요한건지 재문의~ 하기로~~

상담 시나리오
- 챗봇으로 가능? 할 듯
- 일반적인 상담 챗봇이랑 뭐가 다르지?
- 문의~예약까지 자동으로(이건 웬만하면 다 되는데), 상담사 전환까지 agent가?
- 그냥 이 플로우가 가능한건지를 묻는건가?
- 코난에 재문의하기로

---
sales-support
- admin 계정만 권한 관리 기능 있음
- 계정마다 db에 저장해둔 게 다르게 보임..........?
- workflow 루프 돌던거 agent로 바꿈, 자체 retry 3회 걸어두면 error 나와도 혼자 돌아감
- 기업 개수 50개가 안됨, 실제 기업 개수가 그만큼 안될지도~~~
- segment
- 메일 보낼 때 작성하는 메일 주소는 이메일 받을 사람의 메일 주소(위치 조정할 예정)
- AI 수정 요청 (hitl)
- 회사 조사된 내용은 db에 저장되어있음
- 실제로 이메일 전송도 가능함 ('실제 이메일 전송') <- 이걸 hitl로 보기, 사람이 개입해서 휴먼 피드백을 제공
- 눈에 띄게 이상한 부분은 수정할 예정
- 새로고침하면 세션이 풀림?

---
dify에서 n8n 호출하는 걸 하나씩 다해야 되나? 결론 안 남...