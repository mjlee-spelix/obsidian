---
tags: [daily]
date: 2026-05-26
---

## 오늘 할 일

> 5/22 → 5/26 이월(주말 + 5/25 공백) 5트랙 진행. **P1·P2 위임 트랙 종결 ✅**, 별도로 데모(web-ui) 워크플로우 모델 교체 진행. P3 미처리.

### 🔥 P1 — 하네스 일관성 후속 5건 ✅ (모두 위임 종결, SESSION_HISTORY 3엔트리)

> ✅ **5/22 spec 24파일 5/22 코드 정합은 5/22에 이미 완료**. 후속 위임 5건도 5/26 위임 트랙으로 모두 종결.

- [x] **후속 ① conventions.md 정합** — C2/C3/C6/A2/A3 5종 § 신설·보강 (2차 위임)
- [x] **후속 ② Keycloak 동기화 문서화** — **옵션 A 채택**: `references/keycloak-sync.md` (133 lines, § 1~§ 8) 신설 + CLAUDE.md 도메인 지식 표 1행 등록
- [x] **후속 ③ CLAUDE.md 핵심 컨벤션 § 갱신** — KPI 4종 신명 정정 / 페이지 소개 문구 한·영 § 신설 / context-bar **🔒 보류**로 분리 표기 (1차 위임)
- [x] **후속 ④ defect-catalog.md 결함 3건 추가** — H-DASH-21(context-bar 보류, H-DASH-20 자매) / H-INFRA-04(Keycloak 토큰 만료) / H-ENV-04(Git Bash 멀티바이트) (3차 위임)
- [x] **후속 ⑤ scope 외 잔존 폐기명 정합** — `tasks/kpi-cards.md` · `tasks/kpi-drill-through.md` · `requirements/data-mart.md` 40건 신명 치환 (4차 위임)
- [x] **context-bar 폐기→보류 정정** (사용자 정정 사항 추가) — spec 3파일(req/design/tasks) `🔒 폐기` → `🔒 보류` + "임포트/렌더링 제거" → "렌더링만 비활성, 코드 보존" (1.5차 위임)
- [x] **최종 일관성 검증** — grep 12종 자가 검증 통과, `ls web/app/components/admin/context-bar/` drift 가드 통과, 새 drift 0건

#### 5/22 위임에서 발견된 drift 별도 처리 (이월 — 코드 수정 동반)
- [ ] `dashboard/page.tsx:35` defaultValue ↔ `common.json` 값 불일치 (JSON 승)
- [ ] PROMPT 명세 경로 `/api/scripts/` → 실제 `/scripts/` 정정
- [ ] **(P2에서 추가 발견)** `references/keycloak-sync.md` 실제 경로는 `.claude/docs/references/keycloak-sync.md` (한 단계 더 깊음). 향후 위임 프롬프트 정정 박을 것
- [ ] **(P2에서 추가 발견)** Keycloak realm 명 `Spelix` 대문자 S — 위임 문서에서 소문자 `spelix`로 박힌 곳 정정

### 🔥 P2 — web-ui Keycloak 연결 사전 분석 ✅ (위임 종결)

위임: [[0. Inbox/2026-05-26 web-ui Keycloak 통합 사전 분석 위임 (P2)]]

- [x] **분석 톱 4 확정**
  - ① web-ui 스택 — Next.js **16.1.6** App Router + React **19.2.3** + TS 5.9.3, webpack 강제, npm. `frontend/app/login/page.tsx` form UI만 + submit **stub**(sessionStorage 통과형)
  - ③ 백엔드 유무 — **있음**: 별도 Flask(`backend/app.py`, 5 Blueprint). 인증 라우트·미들웨어 0건. Next.js API route는 부분만
  - ⑤~⑦ spx-agent 패턴 — `controllers/console/auth/keycloak.py` (405 lines, 6 엔드포인트) + `libs/passport.py` JWKS RS256 + `ext_login.py:78-96` 분기. **keycloak-js/next-auth 미사용 → 백엔드 자체 PKCE → React 19 충돌 자동 0**
  - ⑧ realm/client — realm `Spelix` 공유, client `web-ui-demo` 전용 신설(public + PKCE)
- [x] **통합 전략 결정** — **SSR + httpOnly cookie 채택** (spx-agent 패턴 그대로 포팅). 토큰 cookie 3종(access/refresh/csrf), silent refresh, KC `/logout` + cookie clear + end_session_endpoint redirect
- [x] **승랑님 base 6 커밋 식별** — `f3e9dbe keycloak sso → c8cc19c → 0ff4434 → d10a5f9 → a687c1a → 3acfba4` (유저명 로그인 + audit 통합 최신)

### 📝 P3 — 외부 커뮤니케이션 ❌ (오늘 0건 처리, 5/27로 이월)

- [ ] 김이사님 Flask-Login 보고 (5/11~ 누적)
- [ ] PM 컨펌 2건 — KPI 1 "신규" 시점 / "이용" 정의 (KPI 3은 1차 이름 확정 컨펌만)
- [ ] **P2 결과 이사님 PM 컨펌 4건 묶음 보고 신규**:
  - ⑨ 데모 계정 발급 방식 — DEMO 그룹 신설 vs 기존 공유 (신설 권장)
  - 통합 전략 B(SSR+httpOnly cookie) 채택 확정 요청
  - client_id 명명: `web-ui-demo` vs `spelix-web-ui` 등 대안
  - web-ui 배포 도메인 → redirect_uri 등록 시점
- [ ] 슬랙 안내 — 잠복 risk 청산 + dev 머지 + dify-audit 재빌드 + 시드 SQL 3건 + Keycloak 동기화 스크립트 안내
- [ ] 권대리님 TS 에러 5건 / 승랑님 진행 공유

### 🔧 P4~P5 — 미처리 (이월)

- [ ] 서버 Dify 업데이트 (192.168.10.194, dev 머지 후) / 서버 .env 누락 12개 머지 / 시드 `created_at` 90일 spread
- [ ] CLAUDE.md 불변식 3번 RBAC prefix(`spx_`) / 응답 키 통일 `items` vs `rows`
- [ ] 지식노트 9종 (5/21 6 + 5/22 3) — [[4. 지식노트]] atomic 분리

## 회의

- 없음 (혼자 작업일)

## 배운 것

- **vLLM 플러그인 text embedding 모델 타입 미지원** → **OpenAI API Compatible 플러그인으로 우회** (model_type=text-embedding 설정해서 vLLM 엔드포인트 그대로 사용). bge-m3-ko 같은 임베딩 모델 띄울 때 Dify에 등록하는 표준 우회 패턴 → **지식노트 후보**
- **vLLM 실행 인자 핵심** — `--tool-call-parser gemma4` / `--reasoning-parser gemma4` 빠지면 raw `<|tool_call>` 토큰 노출. 모델 교체 시 parser 매칭 필수 (gpt-oss → gemma4)
- **Dify 모델 설정 max_context 함정** — 모델이 128K 지원해도 Dify UI에서 `4096`으로 깎으면 입력 길이 초과 시 에러. 모델 한계로 풀어두는 게 안전
- **승랑님 데모 워크플로우 임베딩 모델 정합** — RAG 검색이 옛 Ollama 임베딩 모델 가리켜서 문서 참조 0건이었음. vLLM bge-m3-ko 교체 후 정상 동작 — **임베딩 모델은 인덱싱·검색 양쪽 정합이 필수** (한 쪽만 바꾸면 차원 불일치 또는 모델명 mismatch)

## 메모

### 데모(web-ui) 워크플로우 모델 교체 작업 (혼자 작업, 5/26)

**컨텍스트**: 데모용 워크플로우(승랑님 작성) + n8n 워크플로우가 옛 gpt-oss-120b 사용. 데모 환경 정합 위해 vLLM에서 돌고 있는 gemma-4-26b로 교체.

**Dify 측**
- [x] gpt-oss-120b → gemma-4-26b 교체 — 대부분 정상
- [x] 모델 설정에서 max_context 4096으로 잡아 에러 — 풀어서 해결
- [x] 승랑님 RAG 워크플로우가 옛 Ollama 임베딩 모델(`qwen3-embedding:8b`) 가리켜 문서 참조 0건 → **vLLM `bge-m3-ko`로 교체** (API Base `http://192.168.10.40:8010`, `/v1` 제외, context 8192, max chunks 256)
- [x] vLLM 플러그인 `yangyaofei/vllm` 전 버전(0.1.3~0.2.3) `text_embedding: false` 확정 (manifest.yaml) → **OpenAI-API-compatible 플러그인으로 우회**
- ⚠️ 검색 스코어 0.11~0.13 낮음 — 기존 인덱스가 옛 임베딩 모델로 생성된 벡터라 mismatch. **재인덱싱은 안 함 결정** (잔여 작업 아님, 의도적 폐기)
- ⚠️ **ngrok 관련 에러** — 본인 추가 조사 종료, **담당자 확인 트랙으로 넘김**
  - 에러: `Reached maximum retries (0) for URL https://...ngrok-free.dev/v1/chat-messages` (4월 21일~)
  - **원인 미확정** — 5/27 SESSION_HISTORY entry에 "5001 포트 mismatch"로 박혔으나 **틀린 진단**. ngrok은 Dify API가 아니라 **Dify Web(3000 포트)** 과 연결됨 → 5001로 재시작하면 해결된다는 가정 폐기
  - 본인 더 보지 않음, 담당자에게 넘김

**n8n 측**
- [x] OpenAI credential 설정해서 gemma 쓰게 변경
- [x] 시장 모니터링 워크플로우만 우선 교체
- [ ] ⚠️ **outputparser 에러 발생** — 시장 모니터링에서 모델 응답 파싱 실패. gemma 출력 포맷이 gpt-oss와 달라서 schema 안 맞을 가능성
- [ ] 다른 n8n 워크플로우 전부 교체 필요 — outputparser 해결 후 일괄

**Keycloak (P2 통합 PR 트랙)**
- [ ] 보자........... — 위임 결과(SESSION_HISTORY § P2) 받았으니 통합 PR 12단계 어디부터 박을지 결정 필요. 이사님 PM 컨펌 4건이 가장 막힌 의존성

## 내일 할 일

### 🔥 P1 — 데모 안정화 (어제 미해결분 청산)

- [ ] **n8n outputparser 에러 해결** — 시장 모니터링 워크플로우에서 gemma 응답 파싱 실패. gpt-oss → gemma 응답 포맷 차이 확인 후 schema 또는 prompt 조정
- [ ] **n8n 나머지 워크플로우 전부 gemma로 교체** — outputparser 해결 후 일괄 적용
- [ ] **데모 테스트 (전체 워크플로우)** — Dify + n8n 양쪽 종단 동작 확인
- ~~ngrok 에러 재조사~~ — 담당자 트랙으로 넘김 (본인 추가 조사 안 함)

### 🔥 P2 — Keycloak 통합 PR 시작 (P2 사전 분석 결과 활용)

- [ ] **이사님 PM 컨펌 4건 묶음 보고** (가장 막힌 의존성, 답변 받고 → #1 진행):
  - ⑨ 데모 계정 발급 방식 (DEMO 그룹 신설 vs 기존 공유)
  - 통합 전략 B (SSR+httpOnly cookie) 채택 확정
  - client_id 명명 (`web-ui-demo` vs `spelix-web-ui`)
  - web-ui 배포 도메인 → redirect_uri 등록 시점
- [ ] **통합 PR #1·2 시작** (컨펌 받은 후) — 194 KC admin: web-ui-demo client 신설 + CORS 허용

### 📝 P3 — drift 2건 + P2 추가 발견 2건 정정 (잊지 말 것)

- [ ] `dashboard/page.tsx:35` defaultValue ↔ `common.json` JSON 값 정합
- [ ] PROMPT 명세 경로 `/api/scripts/` → `/scripts/`
- [ ] **(P2 추가 발견)** `keycloak-sync.md` 경로 박힌 곳 모두 `.claude/docs/references/keycloak-sync.md`로 정정 (한 단계 더 깊음)
- [ ] **(P2 추가 발견)** realm 명 `spelix` → `Spelix` 대소문자 정정

### 📝 P4 — 외부 커뮤니케이션 (5/20부터 누적 5일째)

- [ ] 김이사님 Flask-Login 보고 (5/11~, 최우선)
- [ ] PM 컨펌 2건 — KPI 1 "신규" 시점 / "이용" 정의
- [ ] 슬랙 안내 (dev 머지 + dify-audit 재빌드 + 시드 SQL + Keycloak 동기화 스크립트)
- [ ] 권대리님 TS 에러 5건 / 승랑님 진행 공유

### 💡 시간 남으면 / 지식노트

- [ ] **지식노트 신규 후보 (5/26 데모 작업)**:
  - vLLM OpenAI API Compatible 플러그인 우회 패턴 (text embedding 모델 타입 미지원 시)
  - Dify 모델 max_context 함정 (UI 잘못 잡으면 모델 한계 무관하게 에러)
  - 임베딩 모델 교체 시 인덱싱·검색 양쪽 정합 (차원/모델명 mismatch)
- [ ] 지식노트 9종 누적 (5/21 6 + 5/22 3) atomic 분리 — [[4. 지식노트]]
