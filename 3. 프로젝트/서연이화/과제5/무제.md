1. 요청한 다음 응답을 받을때, 내부/외부 섞어서 10개까지 만들수 있게 하는데 화면에서 외부몇개 내부 몇개 지정을 하는 데(원인/대책서) 현재 기본값을 각각 5개씩으로 하고 변경할수 있게 해줘, 응답 받은 다음에 화면에 표시할때는 내부정보 인지 외부정보인지를 구분해서 표시를 해줘,
2. 여러개를 선택하고 나머지는 나머지원인 계속 찾아달라고 하면서 여러개 선택할수 있게 하고
3. 여러사이클을 도는데 최종적으로는 여러번 돌수 있게 한다. 체크한건 남고 추가적인 대책을 여러개 표시를 하고, 
4. 순서별로 표시를 하는데 혹시 유사도에 대한 점수가 있으면 점수를 같이 표시해 주도록 해줘, 
5. 지금 현재 내부 데이터는 호출하기 불가능하니 이부분은 더미데이터로 출력을 해주면 돼
6. 여러개의 원인을 선택하면 선택된 여러개가 표시되고, 대책을 찾아 달라고 하면 선택된 원인마다 대책을 따로따로 찾아 서 해당 원인에 따른 대책을 어러개를 해줘,
7. 현상은 1개, 현상에 따른 원인은 N 개, 그 원인에 따른 대책을 N개를 찾아주는거고, 해당 대책을 다시 찾을때는 선택된 원인별 대책을 따로 찾을수 있게 (버튼을 원인에 따라 각각 )해주면 돼