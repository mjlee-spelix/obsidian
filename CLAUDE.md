# 볼트 소개

이 옵시디언 볼트는 회사 생활 기록용입니다.
사용자는 신입 개발자이며, 업무 내용/학습/회의를 체계적으로 기록하고 있습니다.

# 볼트 구조

- `0. Inbox/` - 빠른 메모, 나중에 정리할 것
- `1. Daily/` - 데일리 노트 (YYYY-MM-DD 형식), 템플릿 참고
- `2. 회의록/` - 회의별 노트, 템플릿 참고
- `3. 프로젝트/` - 진행 중인 프로젝트 (n8n, web-ui 등)
- `4. 지식노트/` - 배운 것 하나당 노트 하나 (atomic note), 템플릿 참고
- `5. 템플릿/` - 노트 템플릿 (수정하지 말 것)
- `이미지/` - 첨부 이미지

# 노트 작성 규칙

## 데일리 노트
- `1. Daily/` 폴더에 생성
- 그날의 요약 허브 역할, 상세 내용은 별도 노트로 분리하고 `[[링크]]`로 연결
- 섹션: 오늘 할 일, 회의, 배운 것, 메모, 내일 할 일

## 지식노트
- `4. 지식노트/` 폴더에 생성
- 하나의 개념/배운 것 = 하나의 노트 (atomic)
- 반드시 tags를 포함 (예: n8n, AI, LLM, dify, 개발, CS)
- `## 관련 노트` 섹션에서 다른 지식노트와 `[[링크]]` 연결 → 그래프 뷰 활용

## 회의록
- `2. 회의록/` 폴더에 생성
- 파일명: `MMDD 회의명` 형식

# 태그 체계

분류는 폴더가 아닌 태그로 합니다:
- `#n8n`, `#dify` - 도구별
- `#AI`, `#LLM`, `#AI-Agent` - AI 관련
- `#개발`, `#CS` - 기술 지식
- `#daily`, `#회의` - 노트 유형

# 작업 요청 시 참고사항

- 데일리 노트에서 지식노트로 분리할 내용을 찾아달라는 요청이 자주 있을 수 있음
- 지식노트 작성 시 기존 노트와의 `[[링크]]` 연결을 항상 고려할 것
- 한국어로 응답할 것
- 마크다운 테이블 안에서 `[[경로|별칭]]` 위키링크 사용 금지 — `|`가 테이블 컬럼 구분자로 파싱됨. 테이블 안에서는 `[[경로]]` 형태만 사용하고, 설명은 링크 뒤에 텍스트로 붙일 것

# 세션 히스토리

## 2026-04-29: SPX-Agent 하네스 문서 체계 구축

- `3. 프로젝트/spx-agent/` = 실제 프로젝트 `.claude/` 폴더에 들어갈 하네스 문서
- RBAC 테이블 담당: **김이사님** (권대리님 아님)
- 부서 구조: **플랫** (parent_id 트리 미사용, 독립 부서)
- Spec 구조: `specs/requirements/`, `specs/design/`, `specs/tasks/` 3파일 분리
- 구현 순서: **프론트(목업) → 백엔드 → 연동 → 테스트**
- Phase A 남은 것: architecture.md, conventions.md, quality-criteria.md (Dify 소스 분석 필요)

## 2026-05-12: 설정 사이드바 토글 기능 KAN-29로 포팅

- 대상 프로젝트: `C:\Users\Administrator\Projects\spx-agent`
- 출처: `feature/KAN-28-AUDIT-LOGS` 브랜치, 커밋 `d989ac4` (감사로그 포트 통합 작업)
- 대상: `KAN-29-admin-dashboard` 브랜치
- 파일: `web/app/components/header/account-setting/index.tsx`
- **cherry-pick 불가** — KAN-28 커밋에 부서/대시보드 메뉴 제거, AUDIT_LOG 추가, `usePeriod` 제거 등 KAN-29와 충돌하는 변경이 섞여 있어 사이드바 토글 부분(+33/-5)만 수동 발췌 적용
- 구현 핵심: `isSidebarCollapsed` `useState` + `i-ri-menu-fold-line`/`unfold-line` 토글 버튼, 폭 `w-[224px] ↔ w-[56px]`, 컨텐츠 `w-[824px] ↔ flex-1`, `transition-all`
- **상태 미저장** (다이얼로그 재오픈 시 펼침으로 초기화) — 필요 시 `useLocalStorageState`로 교체
- ⚠️ KAN-28 브랜치에서 부서관리/대시보드 메뉴 항목이 설정 사이드바에서 제거됨 — 의도/실수 여부 추후 확인 필요
