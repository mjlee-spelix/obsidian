## 로컬 개발 환경 구성

```
# 1. 미들웨어
docker compose -f docker-compose.middleware.yaml up -d

# 2. Flask API
./dev/start-api   # chroma 에러 나도 그냥 진행

# 3. 프론트
cd web && pnpm dev
```

### 1단계: 도커 미들웨어 먼저 띄우기

도커에는 **앱 서버(api/web)는 올리지 않고** 인프라만 올려요:

```bash
cd docker
docker compose -f docker-compose.middleware.yaml up -d
```

올라가는 컨테이너:

|컨테이너|역할|포트|
|---|---|---|
|`db_postgres`|PostgreSQL DB|5432|
|`redis`|캐시/큐|6379|
|`weaviate`|벡터 DB|8080|
|`sandbox`|코드 실행 샌드박스|8194|
|`plugin_daemon`|플러그인 데몬|5002|
|`ssrf_proxy`|SSRF 방지 프록시|3128|

또는 dev 스크립트 사용:

```bash
./dev/start-docker-compose
```

---

### 2단계: 백엔드(API) 실행

```bash
./dev/start-api
```

- Python Flask/Gunicorn 기반 ([api/app.py](vscode-webview://19v0f66hbb118n6fghbv51q7bvlsdmmhfualqnq0hklrlg73ttr0/api/app.py))
- 기동 시 DB 마이그레이션 자동 실행
- 포트: **5001**

---

### 3단계: 프론트엔드(Web) 실행

```bash
./dev/start-web
```

또는 직접:

```bash
cd web
pnpm dev
```

- Next.js 기반
- 포트: **3000**
- 브라우저에서 `http://localhost:3000` 접속

---

### 4단계: (선택) Worker 실행

비동기 작업(AI 요청, 파일 처리 등)이 필요하면:

```bash
./dev/start-worker
```